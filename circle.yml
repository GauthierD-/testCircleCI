machine:
  node:
    version: 6.5.0
  environment:
    NPM_TOKEN: 88ffee85-467a-4581-af54-251c1267faf9
dependencies:
  pre:
    - echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > "${HOME}/.npmrc"
